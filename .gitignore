# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Logs
*.log

# Source video masters - 850 MB, and GitHub rejects any file over 100 MB.
# These live on YouTube (unlisted); the IDs are in data/projects.js.
assets/prototype_videos/

# Working notes and the resume source. Kept out on purpose: every file in this
# repo is served by Pages, so committing these publishes them at guessable URLs
# on the live site. Nothing reads them from GitHub, so they keep working locally.
docs/
CLAUDE.md
